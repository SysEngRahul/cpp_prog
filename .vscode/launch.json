{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current C++ File",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "MIMode": "gdb",
            "preLaunchTask": "Build Active C++ File"
        }
    ]
}
